services:
  daguan-console:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      DAGUAN_PULL_INTERVAL_MINUTES: 30
    volumes:
      - ../data:/app/data
    restart: unless-stopped
