# 更新日志

本项目采用日期加修订号标识本地构建版本。尚未完成浏览器验收的内容放在“待发布”版本中。

## 2026.09.19-r10

### 全库逐题标注 + luna 式多维筛选

- 题库 6183 题全量完成 luna 式标注（题源/考试类别/年份/章节/题型/知识点 313 类/解题方法 23 种/题目形式/难度），产出旁挂 `web/data/annotations.json`，不动原始题库分片、不影响官网同步；
- 线性代数 660 题直接复用 Evan26Ma/liner-algebra-kaoyan-daguan 的 classification 标注（按年份+类别+题干相似度匹配，779 题中匹配 660 题）；高数 3649 题与概率统计 330 题由子代理管线逐题标注（tools/prep_chunks.py 分片、tools/annotate_questions.py 与 tools/merge_luna_tags.py 可复现）；
- 筛选面板重写为 luna 同款两栏布局：左侧十个维度标签、右侧带全库计数的选项卡、底部实时计数与清空/查看题目；维度间取交集、维度内多选，条件持久化；
- 新增「详细筛选」双入口（侧栏 + 顶栏更多菜单）。

### 修复

- 收起侧栏与 1024–1440px 自动图标栏两处侧栏宽度不同步导致压住主内容的问题；
- 「严选」范围在无符合题小节静默回退无提示，现给出 toast；
- 首页推荐小节按钮误开章节菜单（flattenCategoryLeaves 返回包装对象未取 .node）；
- 筛选模块与题目批注功能的数据键冲突（state.annotations 撞名，改为 state.bankTags）；
- 筛选模块误删 sourceMeta 导致未标注题进入过滤链即渲染中断；state.favorites 为 Set 时筛选崩溃；快捷入口维度勾选不生效；
- 复制按钮更名为「复制题目和答案」，复制题干+选项+答案+解析 Markdown；
- 本地服务器静态服务补充 webp/jpg 等 MIME 类型，`/` 路由提供落地页。

### 维护说明

- 现行脚本文件为 `web/app2.js`（历史 app.js/app.main.js 已移除），资源版本随提交更新；
- Service Worker 预缓存升级至 daguan-shell-v43+，如遇旧缓存可用 `/index.html?purge=1` 一键清理。

## 2026.09.19-r9

### 官网落地页复刻

- 新增 `web/landing.html` + `landing.css` + `landing.js`，按官网 cxyonly.fans 最新落地页像素级复刻：顶部博物馆式导航（品牌 Logo + 首页/资源中心/关注我们 + 进度指示）、大字号 Hero 与粒子书页视觉、研学资源中心五学科轮播（考研数学开放，其余按官网状态）、关注获取更多（公众号/服务号二维码与 B 站入口）和备案页脚；
- 落地页设计变量取自官网编译样式：主蓝 `#1550ff`、墨色 `#10131d`、辅灰 `#68768d`、分割线 `#e5eaf3`，导航高 94px、Hero 标题 `clamp(76px, 11.6vw, 168px)/900/-0.04em`、主按钮高 66px，并完整适配 1600/1100/700px 断点；
- 本地中控台 `/` 路由改为提供落地页，`开始学习` 跳转本地题库；静态服务补充 `.webp/.jpg/.jpeg/.gif/.ico` MIME 类型；Service Worker 预缓存清单加入落地页资源（v37）。

### 学习区对齐官网 2026-09 版

- 主题 token 全量对齐官网：`--color-foreground-subtle #647083`、`--color-danger #c33a32`、`--color-border-strong #c8d0dc`，新增 success/warning/info 色板、`--shadow-control/panel/overlay`、`--learning-question-*`（0.75rem 圆角 / 1.25rem 内边距 / 细阴影）、树索引色 `#8b8b8b` 等；
- 首页 Hero 改为官网式「从一道题开始」卡片：推荐小节 + 「练习这一节 / 选择其他章节」+ 动态考研倒计时（2026-12-19，自动计算剩余天数）；
- 侧栏品牌行加入大观园曲线 Logo 与「数学」副标，新增「知识图谱」入口（占位提示），题库范围「核心」更名「严选」并对齐官网实心蓝激活样式；
- 刷题页顶栏对齐官网：浅蓝底 `#f8faff`、蓝色分隔线 `#b4c6e9`、下缘投影，深色主题自动切换；内容栏宽对齐官网 64rem 阅读栏；
- 资源版本号升级（styles v26 / app v29）。

## 2026.09.07-r8

### Windows 发布修复

- 修复部分 Windows PowerShell 5.1 环境下安装脚本的解析失败；
- 安装脚本改为纯 ASCII 内容，并用系统 API 获取开始菜单和桌面路径；
- GitHub Release 新增可直接运行的 `DaguanMath-windows-x64.exe` 及独立 SHA256；
- ZIP 发布包改用 ASCII 文件名，构建时自动解压并复查 PowerShell 语法；
- 统一将运行数据保存到 `%LOCALAPPDATA%\DaguanMath`，避免直接运行 EXE 时在下载目录产生数据副本。

### 新手文档

- GitHub 首页改为优先推荐直接运行的 EXE，并醒目提示普通用户不要下载 Source code；
- 新增 Windows 完全零基础教程，覆盖下载、SmartScreen、启动、刷题、首次同步、AI 配置、更新、备份和常见故障。
- README 开头增加对澄潇宇（帕拉迪宇）的特别鸣谢、B 站头像、使用教程和 17 个大观系列视频的分类直达链接。

## 2026.09.07-r7（待发布）

### 同步流程简化

- 将复杂的计划操作改为清晰的“确认同步”流程；
- 首页只保留“同步进度”一个入口，打开后自动进行只读检查；
- 用一句话显示官网→本地、本地→官网的唯一题目数量；
- 只有涉及官网写入时才要求二次确认；
- 冲突策略、导出和重新配置收进“高级选项”；
- 首次同步改为独立的“首次同步官网数据”流程；
- 增加预览策略、状态版本变化和部分失败后的重新检查处理。

### Windows 发布

- 新增 Windows x64 单文件程序和一键安装脚本；
- 安装包不要求预装 Node.js，会创建桌面和开始菜单快捷方式；
- 增加 GitHub Actions 发布流程，推送 `v*` 标签时自动构建 ZIP 并上传 SHA256 校验文件；
- 补充适用设备、数据位置、安装和首次使用说明。

## 2026.09.07-r6（待发布）

### 修复

- 修复工具区内容已切换但顶部仍高亮“学习区”的问题；
- 修复深层章节目录点击时复用错误层级、导致同级切换路径异常的问题；
- 为章节选择器增加“上一级”和可点击面包屑；
- 删除页面右侧常驻 AI 解答入口，保留题目卡片内入口。

### 改进

- 新增独立官网同步教程，明确首次配置、日常同步和数据安全边界；
- 工具区切换后恢复内容顶部并更新无障碍焦点；
- 加强章节菜单在手机和高缩放环境下的返回路径。

### 详细说明

- [r6 交互修复说明](docs/2026-09-07-r6交互修复说明.md)

## 2026.09.06-r5

### 新增

- 刷题页官网式顶部级联章节选择器；
- 当前小节进度、上一节、下一节和“更多”菜单；
- 父级目录只展开、叶子小节才加载题目；
- 深层目录键盘导航、旧学习位置迁移和响应式底部抽屉。

### 修复

- “求函数表达式”只加载对应 11 题，不再加载“极限”父级的 897 题；
- 刷题页隐藏侧栏，返回首页后恢复侧栏；
- 作答后本节进度即时更新。
